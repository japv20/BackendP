export default class Plate {
    constructor(mealName, mealDescription, mealPrice) {
        this.mealName = mealName;
        this.mealDescription = mealDescription;
        this.mealPrice = mealPrice;
    }
}